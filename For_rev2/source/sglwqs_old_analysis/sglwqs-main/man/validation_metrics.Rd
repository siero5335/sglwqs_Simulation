% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop_methods.R
\name{validation_metrics}
\alias{validation_metrics}
\title{Validation Metrics for sglwqs Predictions}
\usage{
validation_metrics(
  object,
  newdata = NULL,
  outcome = NULL,
  covariates = NULL,
  threshold = 0.5
)
}
\arguments{
\item{object}{A fitted \code{sglwqs} object.}

\item{newdata}{Optional new predictor data.}

\item{outcome}{Optional observed outcome vector, or a column name when
\code{newdata} is a data frame.}

\item{covariates}{Optional covariate data for prediction.}

\item{threshold}{Classification threshold for binomial metrics (default: 0.5).}
}
\value{
A one-row data frame with performance metrics.
}
\description{
Computes predictive performance metrics on validation or external data.
}
\details{
For v1, survey-aware predictive metrics are not implemented. Objects fitted
with survey mode (\code{refit_engine = "svyglm"}) will error rather than
returning ordinary unweighted metrics.
}
\examples{
\donttest{
data("sglwqs_example")
exp_vars <- c(paste0("metal", 1:4), paste0("pesticide", 1:4))
fit_bin <- sglwqs(
  X = sglwqs_example[1:220, exp_vars],
  y = sglwqs_example$outcome_bin[1:220],
  family = "binomial",
  validation = TRUE,
  train_prop = 0.6,
  nfolds = 3,
  nlambda = 20,
  seed = 123,
  verbose = FALSE
)
validation_metrics(fit_bin)
}

}
