% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_methods.R
\name{plot_cv}
\alias{plot_cv}
\alias{plot_cv.sglwqs}
\alias{plot_cv.sglwqs_mids}
\title{Cross-validation Curve for Lambda Selection}
\usage{
plot_cv(object, ...)

\method{plot_cv}{sglwqs}(
  object,
  show_lambda = c("both", "min", "1se", "none"),
  show_se = TRUE,
  point_alpha = 0.6,
  base_size = 11,
  title = "Cross-validation Curve",
  ...
)

\method{plot_cv}{sglwqs_mids}(
  object,
  show_lambda = c("both", "min", "1se", "none"),
  show_se = TRUE,
  point_alpha = 0.6,
  base_size = 11,
  title = "Pooled Cross-validation Curve",
  show_imputations = TRUE,
  ...
)
}
\arguments{
\item{object}{A fitted \code{sglwqs} object.}

\item{...}{Unused.}

\item{show_lambda}{Character. Which reference lines to draw:
\code{"both"} (default), \code{"min"}, \code{"1se"}, or \code{"none"}.}

\item{show_se}{Logical. Show \eqn{\pm 1 SE} ribbon (default: TRUE).}

\item{point_alpha}{Alpha for CV points (default: 0.6).}

\item{base_size}{Base font size (default: 11).}

\item{title}{Plot title.}

\item{show_imputations}{For \code{sglwqs_mids} objects, draw thin
imputation-specific CV curves in the background (default: TRUE).}
}
\value{
A ggplot object.
}
\description{
Visualizes the \code{cv.sparsegl} cross-validation curve stored in a
fitted \code{sglwqs} object.
}
\examples{
data("sglwqs_example")
exp_vars <- c(paste0("metal", 1:4), paste0("pesticide", 1:4))

fit <- sglwqs(
  X = sglwqs_example[, exp_vars],
  y = sglwqs_example$outcome_cont,
  groups = list(
    Metals = paste0("metal", 1:4),
    Pesticides = paste0("pesticide", 1:4)
  ),
  nfolds = 3,
  seed = 123,
  verbose = FALSE
)

plot_cv(fit)

}
