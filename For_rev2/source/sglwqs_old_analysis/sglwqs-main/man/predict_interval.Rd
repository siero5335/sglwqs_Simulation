% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_methods.R
\name{predict_interval}
\alias{predict_interval}
\alias{predict_interval.sglwqs}
\title{Prediction Intervals for sglwqs Objects}
\usage{
predict_interval(object, ...)

\method{predict_interval}{sglwqs}(
  object,
  newdata = NULL,
  covariates = NULL,
  type = c("response", "link"),
  level = 0.95,
  interval = c("confidence", "prediction"),
  ...
)
}
\arguments{
\item{object}{A fitted \code{sglwqs} object.}

\item{...}{Additional arguments passed to \code{predict.sglwqs()}.}

\item{newdata}{Optional predictor data.}

\item{covariates}{Optional covariate data.}

\item{type}{\code{"response"} (default) or \code{"link"}.}

\item{level}{Confidence level (default: 0.95).}

\item{interval}{\code{"confidence"} (default) or \code{"prediction"}.}
}
\value{
A data frame with \code{fit}, \code{lwr}, \code{upr}, and metadata.
}
\description{
Convenience wrapper for interval predictions. When the uncertainty source
is bootstrap matrices, intervals reflect exposure coefficient variation only
(approximate); validation-based intervals account for full model uncertainty.
}
\examples{
data("sglwqs_example")
exp_vars <- c(paste0("metal", 1:4), paste0("pesticide", 1:4))

fit <- sglwqs(
  X = sglwqs_example[, exp_vars],
  y = sglwqs_example$outcome_cont,
  bootstrap = TRUE,
  n_boot = 20,
  keep_boot_matrices = TRUE,
  nfolds = 3,
  seed = 123,
  verbose = FALSE
)

predict_interval(fit, newdata = sglwqs_example[1:5, ], level = 0.95)

}
