% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference_functions.R, R/s3_generics.R
\name{plot_combined_results.sglwqs}
\alias{plot_combined_results.sglwqs}
\alias{plot_combined_results}
\title{Combined Results Plot (Publication-Ready)}
\usage{
\method{plot_combined_results}{sglwqs}(
  object,
  top_n = 10,
  sort_by = c("combined", "selection_freq", "weight"),
  pos_color = "#0072B2",
  neg_color = "#E69F00",
  layout = c("auto", "vertical", "horizontal"),
  show_significance = c("none", "asterisk", "pvalue"),
  conf_level = 0.95,
  base_size = 11,
  include_covariates = FALSE,
  ...
)

plot_combined_results(object, ...)
}
\arguments{
\item{object}{A sglwqs or sglwqs_mids object.}

\item{top_n}{Number of top variables to show per group (default: 10).}

\item{sort_by}{Character. Sorting criterion.}

\item{pos_color}{Color for positive direction (default: "#0072B2").}

\item{neg_color}{Color for negative direction (default: "#E69F00").}

\item{layout}{Layout option for \code{plot_combined_results.sglwqs}:
"auto", "vertical", or "horizontal".}

\item{show_significance}{Significance annotation mode for
\code{plot_combined_results.sglwqs}: "none", "asterisk", or "pvalue".}

\item{conf_level}{Confidence level for confidence-interval based panels
(default: 0.95).}

\item{base_size}{Base font size (default: 11).}

\item{include_covariates}{Logical. Whether to include covariate coefficients
in the validation forest plot panel (default: FALSE).}

\item{...}{Additional arguments passed to methods.}
}
\value{
A combined ggplot object (requires patchwork package).
}
\description{
Generic function to create combined multi-panel plots for publication.
Works with both sglwqs and sglwqs_mids objects.
}
\examples{
\donttest{
data("sglwqs_example")
exp_vars <- c(paste0("metal", 1:4), paste0("pesticide", 1:4))
fit <- sglwqs(
  X = sglwqs_example[1:240, exp_vars],
  y = sglwqs_example$outcome_cont[1:240],
  bootstrap = TRUE,
  n_boot = 10,
  validation = TRUE,
  train_prop = 0.6,
  nfolds = 3,
  nlambda = 20,
  seed = 123,
  verbose = FALSE
)
plot_combined_results(fit)
}

\dontrun{
# For MICE objects
fit_mice <- sglwqs_mice(imp, ...)
plot_combined_results(fit_mice)
}

}
