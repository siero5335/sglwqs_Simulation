% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference_functions.R, R/mice_functions.R,
%   R/s3_generics.R
\name{plot_validation_results.sglwqs}
\alias{plot_validation_results.sglwqs}
\alias{plot_validation_results.sglwqs_mids}
\alias{plot_validation_results}
\title{Plot Validation Results}
\usage{
\method{plot_validation_results}{sglwqs}(
  object,
  conf_level = 0.95,
  include_covariates = FALSE,
  ...
)

\method{plot_validation_results}{sglwqs_mids}(
  object,
  conf_level = 0.95,
  include_covariates = FALSE,
  pos_color = "#0072B2",
  neg_color = "#E69F00",
  show_significance = c("none", "asterisk", "pvalue"),
  base_size = 11,
  title = "Mixture Effect Estimates (Rubin's Rules)",
  ...
)

plot_validation_results(object, ...)
}
\arguments{
\item{object}{A \code{sglwqs} or \code{sglwqs_mids} object with downstream
inference available via \code{validation = TRUE} or \code{refit = "full"}.}

\item{conf_level}{Confidence level (default: 0.95).}

\item{include_covariates}{Whether to include covariate coefficients (default: FALSE).}

\item{...}{Additional arguments passed to methods.}

\item{pos_color}{Color for positive direction (default: "#0072B2").}

\item{neg_color}{Color for negative direction (default: "#E69F00").}

\item{show_significance}{For \code{sglwqs_mids} objects, significance annotation
mode: \code{"none"}, \code{"asterisk"}, or \code{"pvalue"}.}

\item{base_size}{Base font size (default: 11).}

\item{title}{Optional plot title for the pooled validation forest plot.
Use \code{NULL} to suppress the title.}
}
\value{
A ggplot object.
}
\description{
Generic function to visualize validation / mixture-effect estimates.
Works with both \code{sglwqs} and \code{sglwqs_mids} objects.
}
\examples{
\donttest{
data("sglwqs_example")
exp_vars <- c(paste0("metal", 1:4), paste0("pesticide", 1:4))
fit <- sglwqs(
  X = sglwqs_example[1:240, exp_vars],
  y = sglwqs_example$outcome_cont[1:240],
  validation = TRUE,
  train_prop = 0.6,
  nfolds = 3,
  nlambda = 20,
  seed = 123,
  verbose = FALSE
)
plot_validation_results(fit)
}

\dontrun{
# Rubin-pooled downstream forest plot for multiply imputed fits
fit_mice <- sglwqs_mice(imp, ..., refit = "full")
plot_validation_results(fit_mice)
}

}
