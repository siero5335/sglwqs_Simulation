% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop_methods.R
\name{calibrate}
\alias{calibrate}
\alias{calibrate.sglwqs}
\alias{calibrate.sglwqs_mids}
\title{Calibration Table for Binomial sglwqs Models}
\usage{
calibrate(object, ...)

\method{calibrate}{sglwqs}(
  object,
  newdata = NULL,
  outcome = NULL,
  covariates = NULL,
  n_bins = 10,
  ...
)

\method{calibrate}{sglwqs_mids}(
  object,
  newdata = NULL,
  outcome = NULL,
  covariates = NULL,
  n_bins = 10,
  ...
)
}
\arguments{
\item{object}{A fitted \code{sglwqs} object with \code{family = "binomial"}.}

\item{...}{Additional arguments passed to methods.}

\item{newdata}{Optional new predictor data.}

\item{outcome}{Optional observed outcome vector, or a column name when
\code{newdata} is a data frame.}

\item{covariates}{Optional covariate data for prediction.}

\item{n_bins}{Number of prediction bins (default: 10).}
}
\value{
A data frame with bin-level calibration summaries.
}
\description{
Creates a calibration table by grouping predictions into quantile bins.
}
\details{
Survey-aware calibration is not currently implemented. Objects fitted with
survey mode (\code{refit_engine = "svyglm"}) will error rather than
returning ordinary unweighted calibration summaries.
}
\examples{
\donttest{
data("sglwqs_example")
exp_vars <- c(paste0("metal", 1:4), paste0("pesticide", 1:4))
fit_bin <- sglwqs(
  X = sglwqs_example[1:220, exp_vars],
  y = sglwqs_example$outcome_bin[1:220],
  family = "binomial",
  validation = TRUE,
  train_prop = 0.6,
  nfolds = 3,
  nlambda = 20,
  seed = 123,
  verbose = FALSE
)
cal <- calibrate(fit_bin, n_bins = 6)
plot(cal)
}

}
