% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3_generics.R
\name{inference_table}
\alias{inference_table}
\alias{inference_table.sglwqs}
\alias{inference_table.sglwqs_mids}
\title{Create Inference Table for Publication}
\usage{
inference_table(object, ...)

\method{inference_table}{sglwqs}(object, min_freq = 0, top_n = NULL, conf_level = 0.95, ...)

\method{inference_table}{sglwqs_mids}(object, min_freq = 0, top_n = NULL, conf_level = 0.95, ...)
}
\arguments{
\item{object}{A sglwqs or sglwqs_mids object.}

\item{...}{Additional arguments passed to methods.}

\item{min_freq}{Minimum selection frequency to include (default: 0).}

\item{top_n}{Number of top variables to show (default: NULL for all).}

\item{conf_level}{Confidence level (default: 0.95).}
}
\value{
A data frame with inference results, formatted for publication.
The output includes weights, selection frequencies, and validation results
(if available) as attributes.
}
\description{
Generic function to create publication-ready inference tables.
Works with both sglwqs and sglwqs_mids objects.
}
\examples{
\donttest{
data("sglwqs_example")
exp_vars <- c(paste0("metal", 1:4), paste0("pesticide", 1:4))
fit <- sglwqs(
  X = sglwqs_example[1:240, exp_vars],
  y = sglwqs_example$outcome_cont[1:240],
  bootstrap = TRUE,
  n_boot = 10,
  validation = TRUE,
  train_prop = 0.6,
  nfolds = 3,
  nlambda = 20,
  seed = 123,
  verbose = FALSE
)
inference_table(fit)
}

\dontrun{
# For MICE objects
fit_mice <- sglwqs_mice(imp, ...)
inference_table(fit_mice)
}

}
