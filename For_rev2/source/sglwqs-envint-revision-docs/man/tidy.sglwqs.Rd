% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop_methods.R
\name{tidy.sglwqs}
\alias{tidy.sglwqs}
\title{Tidy Summary for sglwqs Objects}
\usage{
\method{tidy}{sglwqs}(
  x,
  what = c("weights", "coefficients", "validation", "bootstrap"),
  direction = c("both", "positive", "negative"),
  conf.int = FALSE,
  conf.level = 0.95,
  ...
)
}
\arguments{
\item{x}{A fitted \code{sglwqs} object.}

\item{what}{Which component to tidy: \code{"weights"} (default),
\code{"coefficients"}, \code{"validation"}, or \code{"bootstrap"}.}

\item{direction}{Direction filter: \code{"both"} (default),
\code{"positive"}, or \code{"negative"}.}

\item{conf.int}{Logical. Include confidence intervals when available.}

\item{conf.level}{Confidence level used when \code{conf.int = TRUE}.}

\item{...}{Unused.}
}
\value{
A data frame in tidy format.
}
\description{
Returns model results in a tidy data-frame format compatible with
broom-style workflows.
}
\examples{
\donttest{
data("sglwqs_example")
exp_vars <- c(paste0("metal", 1:4), paste0("pesticide", 1:4))
fit <- sglwqs(
  X = sglwqs_example[1:180, exp_vars],
  y = sglwqs_example$outcome_cont[1:180],
  bootstrap = TRUE,
  n_boot = 8,
  nfolds = 3,
  nlambda = 20,
  seed = 123,
  verbose = FALSE
)
generics::tidy(fit, what = "weights")
generics::tidy(fit, what = "bootstrap", conf.int = TRUE)
}

}
