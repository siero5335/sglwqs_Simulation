% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_methods.R
\name{confint.sglwqs}
\alias{confint.sglwqs}
\title{Confidence Intervals for sglwqs Objects}
\usage{
\method{confint}{sglwqs}(
  object,
  parm = NULL,
  level = 0.95,
  type = NULL,
  direction = c("both", "positive", "negative"),
  ...
)
}
\arguments{
\item{object}{A fitted \code{sglwqs} object.}

\item{parm}{Optional term names (or numeric indices) to extract.}

\item{level}{Confidence level (default: 0.95).}

\item{type}{Character. \code{"validation"} or \code{"bootstrap"}.
If \code{NULL}, the method prefers \code{"validation"} when available,
otherwise \code{"bootstrap"}.}

\item{direction}{Character. \code{"both"} (default), \code{"positive"},
or \code{"negative"}; used for \code{type = "bootstrap"}.}

\item{...}{Unused.}
}
\value{
A two-column matrix with lower and upper confidence limits.
}
\description{
Returns confidence intervals for validation estimates or bootstrap coefficients.
}
\examples{
data("sglwqs_example")
exp_vars <- c(paste0("metal", 1:4), paste0("pesticide", 1:4))

fit_boot <- sglwqs(
  X = sglwqs_example[, exp_vars],
  y = sglwqs_example$outcome_cont,
  bootstrap = TRUE,
  n_boot = 20,
  nfolds = 3,
  seed = 123,
  verbose = FALSE
)

confint(fit_boot, type = "bootstrap", direction = "positive")

}
