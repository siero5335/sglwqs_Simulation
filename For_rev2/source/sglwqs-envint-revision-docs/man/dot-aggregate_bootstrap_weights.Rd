% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.aggregate_bootstrap_weights}
\alias{.aggregate_bootstrap_weights}
\title{Aggregate Bootstrap Weights (Mean of Per-Draw Weights)}
\usage{
.aggregate_bootstrap_weights(
  boot_pos_coef,
  boot_neg_coef,
  boot_success,
  var_names,
  groups = NULL,
  handle_collinearity = "net"
)
}
\arguments{
\item{boot_pos_coef}{Bootstrap positive coefficient matrix (n_boot x p).}

\item{boot_neg_coef}{Bootstrap negative coefficient matrix (n_boot x p).}

\item{boot_success}{Logical vector of length n_boot.}

\item{var_names}{Character vector of variable names.}

\item{groups}{Optional named list of groups.}

\item{handle_collinearity}{Passed to \code{calculate_weights()}.}
}
\value{
A list with \code{pos_weights} and \code{neg_weights}.
}
\description{
For each successful bootstrap draw, converts raw coefficients into
normalised weights using \code{calculate_weights()}, then averages those
weight vectors across draws.  This is the estimator described in the
manuscript (mean of weights, not weights-of-mean-coefficients).
}
\keyword{internal}
