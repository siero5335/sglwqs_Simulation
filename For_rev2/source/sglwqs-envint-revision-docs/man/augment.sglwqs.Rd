% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop_methods.R
\name{augment.sglwqs}
\alias{augment.sglwqs}
\title{Augment Data with sglwqs Predictions}
\usage{
\method{augment}{sglwqs}(
  x,
  data = NULL,
  newdata = NULL,
  covariates = NULL,
  outcome = NULL,
  type.predict = c("response", "link"),
  ...
)
}
\arguments{
\item{x}{A fitted \code{sglwqs} object.}

\item{data}{Optional data frame used both as predictors and output base.}

\item{newdata}{Optional data frame of predictors.}

\item{covariates}{Optional covariate data for prediction.}

\item{outcome}{Optional outcome column name (character) or numeric vector
for residual calculation.}

\item{type.predict}{Prediction scale: \code{"response"} (default) or \code{"link"}.}

\item{...}{Unused.}
}
\value{
A data frame with added columns:
\code{.fitted}, \code{.wqs_pos}, \code{.wqs_neg}, and optionally \code{.resid}.
}
\description{
Adds fitted values and WQS indices to a dataset.
}
\examples{
\donttest{
data("sglwqs_example")
exp_vars <- c(paste0("metal", 1:4), paste0("pesticide", 1:4))
fit <- sglwqs(
  X = sglwqs_example[1:160, exp_vars],
  y = sglwqs_example$outcome_cont[1:160],
  nfolds = 3,
  nlambda = 20,
  seed = 123,
  verbose = FALSE
)
generics::augment(
  fit,
  data = sglwqs_example[1:10, exp_vars]
)
}

}
