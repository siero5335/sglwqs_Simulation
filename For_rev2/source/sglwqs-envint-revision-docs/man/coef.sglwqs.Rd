% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_methods.R
\name{coef.sglwqs}
\alias{coef.sglwqs}
\title{Coefficients for sglwqs Objects}
\usage{
\method{coef}{sglwqs}(
  object,
  what = c("coefficients", "weights", "all"),
  direction = c("both", "positive", "negative"),
  include_intercept = TRUE,
  include_covariates = TRUE,
  ...
)
}
\arguments{
\item{object}{A fitted \code{sglwqs} object.}

\item{what}{Character. One of \code{"coefficients"}, \code{"weights"},
or \code{"all"}.}

\item{direction}{Character. \code{"both"} (default), \code{"positive"},
or \code{"negative"}.}

\item{include_intercept}{Logical. Include intercept when \code{what}
includes coefficients (default: TRUE).}

\item{include_covariates}{Logical. Include covariate coefficients when
\code{what} includes coefficients (default: TRUE).}

\item{...}{Unused.}
}
\value{
A named numeric vector, or a list with \code{$coefficients} and
\code{$weights} when \code{what = "all"}.
}
\description{
Extracts directional coefficients or weights as named vectors.
}
\examples{
data("sglwqs_example")
exp_vars <- c(paste0("metal", 1:4), paste0("pesticide", 1:4))

fit <- sglwqs(
  X = sglwqs_example[, exp_vars],
  y = sglwqs_example$outcome_cont,
  nfolds = 3,
  seed = 123,
  verbose = FALSE
)

coef(fit, what = "weights", direction = "positive")
coef(fit, what = "coefficients", direction = "both")

}
