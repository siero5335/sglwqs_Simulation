% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference_functions.R, R/mice_functions.R
\name{plot_selection_frequency}
\alias{plot_selection_frequency}
\alias{plot_selection_frequency.sglwqs}
\alias{plot_selection_frequency.sglwqs_mids}
\title{Plot Selection Frequency}
\usage{
plot_selection_frequency(object, ...)

\method{plot_selection_frequency}{sglwqs}(
  object,
  top_n = 15,
  min_freq = 0.1,
  direction = c("both", "positive", "negative"),
  sort_by = c("frequency", "group", "name"),
  facet_by_group = NULL,
  show_threshold = TRUE,
  ...
)

\method{plot_selection_frequency}{sglwqs_mids}(
  object,
  top_n = 15,
  min_freq = 0.1,
  direction = c("both", "positive", "negative"),
  sort_by = c("frequency", "combined", "weight"),
  facet_by_group = NULL,
  show_threshold = TRUE,
  source = c("mi", "bootstrap"),
  pos_color = "#0072B2",
  neg_color = "#E69F00",
  base_size = 11,
  title = NULL,
  ...
)
}
\arguments{
\item{object}{A fitted \code{sglwqs} or \code{sglwqs_mids} object.}

\item{...}{Additional arguments passed to methods.}

\item{top_n}{Number of top variables to show per group/direction (default: 15).}

\item{min_freq}{Minimum selection frequency to display (default: 0.1).}

\item{direction}{Which direction to plot: "both", "positive", or "negative".}

\item{sort_by}{How to sort variables: "frequency" (default), "group", or "name".}

\item{facet_by_group}{Whether to create separate panels for each chemical group (default: TRUE if groups exist).}

\item{show_threshold}{Whether to show the 50\% threshold line (default: TRUE).}

\item{source}{For \code{sglwqs_mids} objects, selection-frequency source:
\code{"mi"} (default) for across-imputation selection frequency or
\code{"bootstrap"} for pooled bootstrap selection frequency.}

\item{pos_color}{Color for positive direction.}

\item{neg_color}{Color for negative direction.}

\item{base_size}{Base font size.}

\item{title}{Optional plot title. When \code{NULL}, a default title is used.}
}
\value{
A ggplot object.
}
\description{
Generic function to visualize selection frequencies for fitted
\code{sglwqs} and \code{sglwqs_mids} objects.
}
\details{
For \code{sglwqs} objects, this reproduces the bootstrap-frequency bar plot.
For \code{sglwqs_mids} objects, a pooled butterfly plot of MI or pooled
bootstrap selection frequency is returned.
}
\examples{
\donttest{
data("sglwqs_example")
exp_vars <- c(paste0("metal", 1:4), paste0("pesticide", 1:4))
groups <- list(
  Metals = paste0("metal", 1:4),
  Pesticides = paste0("pesticide", 1:4)
)
fit <- sglwqs(
  X = sglwqs_example[1:200, exp_vars],
  y = sglwqs_example$outcome_cont[1:200],
  groups = groups,
  bootstrap = TRUE,
  n_boot = 10,
  nfolds = 3,
  nlambda = 20,
  seed = 123,
  verbose = FALSE
)
plot_selection_frequency(fit)
plot_selection_frequency(fit, direction = "positive", top_n = 10)
plot_selection_frequency(fit, facet_by_group = TRUE, sort_by = "frequency")
}

}
